[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "ffmpeg"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "python -m pip install pyTelegramBotAPI requests yt-dlp"
]

[start]
cmd = "python demo.py"
